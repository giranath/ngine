#include "asset_registry.hpp"

namespace ng
{

std::shared_ptr<asset> asset_registry::load(std::string_view asset_path)
{
    // TODO: Load asset_path
    // 1. Get package path
    // 2. Get asset subpath
    // 3. Check if package is already loaded
    // 4. Then check if asset exists for package

    return nullptr;
}

}